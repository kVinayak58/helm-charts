name: CI

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main

jobs:
  checks:
    uses: kVinayak58/platform-config/.github/workflows/helm-lint-ci.yml@main
